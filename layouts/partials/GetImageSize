{{ $layout := .layout }}
{{ $type := .type }}
{{ $sizes := site.Params.image_sizes.sections.pages.item }}

{{ with index site.Params.image_sizes.sections $type }}
  {{ with index . $layout }}
    {{ $sizes = . }}
  {{ end }}
{{ end }}

{{ if .sizes }}
{{ $type := .type }}
  {{ $sizes = .sizes }}
{{ end }}

{{ return $sizes }}