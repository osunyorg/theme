{{ $isNeeded := false }}

{{ if eq .context.Kind "programs" }}
  {{ $isNeeded = true }}
{{ end }}

{{ if gt (len .context.Params.blocks) 1 }}
  {{ $isNeeded = true }}
{{ end }}

{{ return $isNeeded }}