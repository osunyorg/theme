{{- $paths := slice -}}
{{- range .Site.Sections -}}
  {{- $paths = $paths | append .Params.url -}}
  {{- range $index, $page := (where .Site.Pages "Section" .Type) -}}
    {{- if lt $index 2 -}}
      {{- with $page.Params.url -}}
        {{- $paths = $paths | append . -}}
      {{- end -}}
    {{- end -}}
  {{- end -}}
{{- end -}}
{{- $paths | jsonify -}}

